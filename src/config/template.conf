# nuui config

indicator_style = border

bind_insert = i
bind_visual = v
bind_left = h
bind_right = l
bind_up = k
bind_down = j
bind_word_next = w
bind_word_prev = b
bind_line_start = 1
bind_line_end = 0
bind_select_all = a
bind_file_bounds = g
bind_delete = d
bind_copy = y
bind_paste = p
bind_undo = u
bind_redo = r
bind_save = s
